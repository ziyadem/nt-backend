    users 
    
    courses
          FOREIGN KEY user_id
    fruits
          FOREIGN KEY user_id
    animals
          FOREIGN KEY user_id
    cars
          FOREIGN KEY user_id
          FOREIGN KEY company_id
    company
          FOREIGN KEY user_id
    employess
          FOREIGN KEY user_id
          FOREIGN KEY company_id
    customers
          FOREIGN KEY employe_id
          FOREIGN KEY company_id
          FOREIGN KEY car_id
